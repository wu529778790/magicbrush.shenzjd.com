services:
  baoyuimages:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=file:/app/data/baoyuimages.db
    restart: unless-stopped
